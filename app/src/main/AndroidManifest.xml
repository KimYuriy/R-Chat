<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RChat"
        tools:targetApi="31">
        <activity
            android:name=".windows.settings.additional_user_info.AdditionalUserInfoWindow"
            android:exported="false" />
        <activity
            android:name=".windows.search.SearchUsersWindow"
            android:exported="false" />
        <activity
            android:name=".windows.messages.MessagesWindow"
            android:exported="false" />
        <activity
            android:name=".windows.chats.ChatsWindow"
            android:exported="false" />
        <activity
            android:name=".windows.registration.RegisterWindow"
            android:exported="false" />
        <activity
            android:name=".windows.authorization.AuthWindow"
            android:exported="false" />
        <activity
            android:name=".windows.splash.SplashWindow"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>